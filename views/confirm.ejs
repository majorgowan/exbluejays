<!DOCTYPE html>
<html lang="en">

<%- include("partials/header.ejs") %>

<body>
<div class="exjayform">
    <% if (retry) { %>
        <p><%= message %></p>
        <p>Please <a href="/subscribe?email=<%= email %>">click here</a> to subscribe again.</p>
    <% } else { %>
        <p><%= message %></p>
    <% } %>
</div>
</body>

</html>
